import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.dallas8000_ops.caydenjoy',
  appName: 'CaydenJoy',
  webDir: 'dist'
};

export default config;
